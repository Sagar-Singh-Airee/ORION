"""Optional self-supervised pretraining is deliberately not run by ORION's baseline workflow.

Implement and execute MAE/CLIP experiments only after the supervised, leakage-safe
cross-validation baseline is established in Kaggle.
"""
