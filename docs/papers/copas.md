# Multi-view MRI aggregation

Use a multi-view extension only after confirming that the selected series and
orientation metadata are reliable. Study-level patient grouping remains required
regardless of the number of views.
