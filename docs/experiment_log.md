# Experiment log

Record only reproducible runs. Each row should contain the Git revision,
config path and overrides, fold IDs, seed, validation macro AUC, per-label AUC,
hardware, data revision, and the exact checkpoint path.

Do not use leaderboard observations as selection evidence. Add an entry only
after out-of-fold predictions and leakage checks are saved.
